# Scene model

Schemas live in [`packages/fixtures/schemas`](../../packages/fixtures/schemas). The `SceneDocument` is the single serializable scene shape used by both localStorage autosave and JSON import/export.

See `PLAN.md` § Scene data model.
